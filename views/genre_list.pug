extends layout 

block content 
    h1(class='genrelisttitle')= title

    ul 
        each genre in genre_list 
            li 
                a(href=genre.url) #{genre.name}
        
        else 
            li There are no artists.